# Prerequisites Plan: 6 Tasks — Full Type Safety, Zero any, env hygiene, migrations

**Context**: MASTER_MISSION_BRIEF.md Phases 1-10 are already done. Now executing 6 prerequisite tasks to harden the codebase before Phase 11.

## Task 1: Upgrade DI Container ✅ (DONE by subagent general-40)

**File**: `server/src/lib/container.ts`

- Fully typesafe replacement: `ContainerToken<T>` (branded string), `ContainerError`, `registerFactory`, `registerSingleton`, `registerScoped`, `resolve<T>`, `resolveAll<T>(pattern)`, `createScope()`, `reset()`
- Zero `: any`, zero new tsc errors
- ✅ Verified

## Task 2 + 3: PipelineBuilder + API Client Typing 🟡 (general-41 running)

**Files**: `src/pages/PipelineBuilder.tsx`, `src/lib/api-client.ts`, `src/lib/api-types.ts`, `src/lib/types.ts`

- Step A: Remove all `localStorage.getItem/setItem` from PipelineBuilder → use `apiClient.listPipelines()/getPipeline/createPipeline`
- Step B: Create `api-types.ts` with `ApiMemory`, `ApiSkill`, `ApiProject`, `ApiNote`, `ApiRecallResult`, `ApiAuditEntry`, `ApiHealth`, `ApiEnvelope<T>`, `Pipeline`, `PipelineNode`, `PipelineEdge`, `PipelineInput`
- Step C: Type `api-client.ts` — replace ALL `: any` with typed returns via `ApiEnvelope<T>`
- Step D: Add pipeline CRUD methods to apiClient
- **Verify**: `grep -c ': any' src/lib/api-client.ts` = 0, `grep -rn 'localStorage' src/pages/PipelineBuilder.tsx` = 0, tsc passes

## Task 4: Fix Raw `process.env` Reads 🟢 (Partially done)

### 4a env.ts schema + desktop-actuator 🟡 (general-42 running)

- Add to `env.ts`: `NEXUS_GUI_MODE`, `DESKTOP_ACTUATOR_MODE`
- Replace `process.env.NEXUS_GUI_MODE` → `getEnv().NEXUS_GUI_MODE` in `desktop-actuator.ts`

### 4b federated-recall + wasm 🟡 (general-43 running)

- Add to `env.ts`: `NEXUS_PLUGIN_PUBLISHER_PUBKEYS`
- Replace `process.env[envKey]` in federated-recall + wasm-plugin-runtime

### 4c scheduler + m3 ✅ (DONE by general-44)

- `NEXUS_SCHEDULER_MAX_CONCURRENT` → env.ts ✅, `AIONRS_BASE_URL` → env.ts ✅

## Task 5: Database Migrations 🟡 (general-45 running)

**Files**: `server/src/db/migrations/0047_audit_log_append_only.sql`, `server/src/db/migrations/0048_vector_hnsw_indexes.sql`

- If PostgreSQL: run migrations, verify audit_log trigger + HNSW indexes
- If SQLite (default): document why 0047 is skipped, verify FTS5 coverage for memories/skills/notes

## Task 6: Zero `: any` 🟡 (Multiple agents running)

### 6a DbTx type 🟡 (general-46 running)

- Create `DbTx` type in `server/src/db/client.ts`
- Replace all `tx?: any` → `tx?: DbTx` in server/src/

### 6b server/src/lib/ 🟡 (general-47 running)

- Fix `: any` in: `rate-limit.ts`, `audit.ts`, `security.ts`, `hono-env.ts`

### 6c server/src/services/ 🟡 (general-48 running)

- Fix all `: any` in services: catch blocks (`e: unknown`), db parameters, input types

### 6d src/ frontend 🟡 (general-49 running)

- Fix all `: any` in pages/, components/, lib/: event handlers, API responses, props, state

## Final Verification (run after ALL tasks complete)

```
1. cd server && npx tsc --noEmit          # zero errors
2. cd .. && npx tsc --noEmit              # zero errors
3. grep -rn ': any' server/src/ src/       # zero matches
4. grep -rn "process\.env\[" server/src/   # only self-improvement-harness.ts
5. grep -rn 'localStorage' src/pages/      # zero matches
6. cd server && npm run dev                # boots without warnings
```

## Execution Plan

- Tasks 1-5 are independent → ran in parallel (10 subagents)
- Task 6 runs partially in parallel with 1-5 (agents handle non-overlapping files)
- After all agents complete: run final verification script, fix any remaining issues, call plan_exit

## Remaining Gap Found During Planning

### packages/sdk TS compile error

- `packages/sdk/src/types.ts` re-exports from `./bindings` which doesn't exist
- `packages/sdk/src/acp.ts` imports `Message`, `ToolDefinition`, `ChatOptions`, `HealthStatus` from types but they never resolve
- Root tsconfig includes `packages/**/*.ts` so `npx tsc --noEmit` at root fails
- Fix: Create `packages/sdk/src/bindings.ts` with clean TypeScript interfaces for Message, ToolDefinition, ChatOptions, HealthStatus
- Rust ts-rs crate is orphaned (no FFI bridge); generating actual Rust bindings is overkill
