> ⚠️ **READ `NOT_NEXUS.md` FIRST.** This `docs/omniroute/` tree is a **vendored third-party
> project (OmniRoute by diegosouzapw)** and is **NOT part of NEXUS 2.0 / Agentic OS V3**.
> It contains its own docs, CLI, and third-party security-circumvention material under
> `security/`. Do not treat any of it as NEXUS documentation. Sentinel is security-reviewing
> `security/`.

# OmniRoute (vendored — external project)

This is the upstream documentation for **OmniRoute**, an independent LLM routing proxy. It is
included here only as a reference source. For the authoritative content, see the upstream
OmniRoute project.

NEXUS 2.0's own (unrelated) routing adapter is `server/src/services/omniroute-bridge.ts`.
